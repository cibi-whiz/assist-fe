
import Header from '../../../components/Header'

const AssistUsers = () => {
  return (
    <div className='space-y-4'>
        <Header handleRefresh={() => {}} isRefreshing={false} handleExport={() => {}} tableData={[]} ns="assistUsers" />
    </div>
  )
}

export default AssistUsers